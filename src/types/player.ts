export type Player = {
  name: string;        // ✅ NEW
  level: number;
  exp: number;
  hp: number;
  maxHp: number;
  gold: number;
  energy: number;      // ⚡ new resource used for combat
  avatar: string;

  
};
